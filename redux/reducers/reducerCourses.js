import COURSES from "../../data/testData";

const initialState = {
    existingCourses: COURSES
}

//on alimente le state avec l'array et on retourne un new state
const reducerCourses = (state = initialState, action) => {
    return state
}

export default reducerCourses;